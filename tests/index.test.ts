import { Utilities } from "../Utilities";

describe("A Utility Class", () => {
   beforeAll(() => {
   });
   describe("With a Dot Product Inner Class", () => {
	  test.todo("Should perform the operation.");
	  test.todo("Should be a number.");
	  test.todo("Should throw the error.");
   });
});
